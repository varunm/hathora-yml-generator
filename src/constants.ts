export enum PRIMITIVES {
    STRING = "string",
    INT = "int",
    FLOAT = "float",
    BOOLEAN = "boolean",
}

export enum TYPES {
    OBJECT = "object",
    ENUM = "enum",
    ALIAS = "alias",
    UNION = "union",
}
